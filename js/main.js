// Initialize all animations and features
document.addEventListener('DOMContentLoaded', () => {
    // Initialize navigation
    initNavigation();
    
    // Initialize language scroll animation
    initLanguageScroll();
    
    // Initialize IP Tracker animations
    initIPTrackerAnimation();
}); 